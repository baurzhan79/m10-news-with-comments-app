import React from "react";
import "./Toolbar.css";

const Toolbar = () => (
    <header className="Toolbar">
        <div className="Toolbar-logo">
            <span>
                News
            </span>
        </div>
    </header>
);

export default Toolbar;